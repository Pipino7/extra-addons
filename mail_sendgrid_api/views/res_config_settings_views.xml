<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Extensión de la vista de configuración general (Compatible Odoo Community 18) -->
        <record id="res_config_settings_view_form_inherit_sendgrid" model="ir.ui.view">
            <field name="name">res.config.settings.form.inherit.sendgrid</field>
            <field name="model">res.config.settings</field>
            <field name="inherit_id" ref="base.res_config_settings_view_form"/>
            <field name="arch" type="xml">

                <!-- En Odoo Community 18 insertamos directamente dentro del <form> principal -->
                <xpath expr="//form" position="inside">

                    <!-- Separador visual -->
                    <separator string="📧 SendGrid API" class="mt-4"/>

                    <!-- Activar/Desactivar SendGrid -->
                    <group>
                        <field name="sendgrid_enabled" string="Usar SendGrid para envío de emails"/>
                    </group>

                    <!-- Configuración de credenciales -->
                    <group string="Configuración de SendGrid" attrs="{'invisible': [('sendgrid_enabled', '=', False)]}">
                        <group>
                            <field name="sendgrid_api_key" 
                                   password="True" 
                                   placeholder="SG.xxxxxxxxxxxxxxxxxx"
                                   string="API Key de SendGrid"
                                   required="True"
                                   attrs="{'required': [('sendgrid_enabled', '=', True)]}"/>
                            <field name="sendgrid_default_from_email" 
                                   placeholder="no-reply@tudominio.com"
                                   string="Email Remitente por Defecto"/>
                        </group>
                        <group>
                            <field name="sendgrid_default_from_name" 
                                   placeholder="Mi Empresa"
                                   string="Nombre Remitente por Defecto"/>
                        </group>
                    </group>

                    <!-- Botón de prueba -->
                    <div class="row mt16" attrs="{'invisible': [('sendgrid_enabled', '=', False)]}">
                        <div class="col-12">
                            <button name="action_test_sendgrid_connection"
                                    string="🧪 Probar Conexión con SendGrid"
                                    type="object"
                                    class="btn-primary"
                                    help="Envía un email de prueba para verificar que la configuración es correcta"/>
                        </div>
                    </div>

                    <!-- Información y ayuda -->
                    <div class="alert alert-info mt-3" role="alert" attrs="{'invisible': [('sendgrid_enabled', '=', False)]}">
                        <h4 class="alert-heading">ℹ️ Acerca de SendGrid</h4>
                        <p>
                            <strong>SendGrid API</strong> permite enviar emails mediante HTTPS (puerto 443)
                            en lugar de SMTP (puerto 25/587), ideal para servidores con SMTP bloqueado.
                        </p>
                        <hr/>
                        <p class="mb-2"><strong>✅ Ventajas:</strong></p>
                        <ul class="mb-2">
                            <li>No requiere configuración de SMTP</li>
                            <li>Compatible con DigitalOcean, AWS, Google Cloud, etc.</li>
                            <li>Más rápido y confiable que SMTP</li>
                            <li>Estadísticas y tracking disponibles en SendGrid</li>
                            <li>Mejor gestión de errores y reintentos</li>
                        </ul>
                        <hr/>
                        <p class="mb-0">
                            <strong>� Obtén tu API Key:</strong>
                            <a href="https://app.sendgrid.com/settings/api_keys" target="_blank" class="alert-link">
                                SendGrid API Keys
                            </a>
                        </p>
                        <p class="mb-0">
                            <strong>📚 Documentación:</strong>
                            <a href="https://docs.sendgrid.com/api-reference/mail-send/mail-send" target="_blank" class="alert-link">
                                SendGrid Mail Send API
                            </a>
                        </p>
                    </div>

                </xpath>

            </field>
        </record>

    </data>
</odoo>
