<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Configuración de SendGrid (compatible Odoo 17/18 Community) -->
        <record id="res_config_settings_view_form_inherit_sendgrid" model="ir.ui.view">
            <field name="name">res.config.settings.form.inherit.sendgrid</field>
            <field name="model">res.config.settings</field>
            <field name="inherit_id" ref="base.res_config_settings_view_form"/>
            <field name="arch" type="xml">

                <xpath expr="//form" position="inside">

                    <separator string="SendGrid API"/>

                    <group col="2" string="Configuración de SendGrid">
                        <field name="sendgrid_enabled"/>
                        <field name="sendgrid_api_key" password="True"/>
                        <field name="sendgrid_default_from_email"/>
                        <field name="sendgrid_default_from_name"/>
                    </group>

                    <div class="mt16">
                        <button name="action_test_sendgrid_connection"
                                string="🧪 Probar Conexión"
                                type="object"
                                class="btn btn-primary"
                                style="margin-top: 10px;"/>
                    </div>

                    <div class="text-muted mt16">
                        <p>
                            <strong>ℹ️ Nota:</strong> Este módulo usa la <b>API HTTPS de SendGrid</b> para enviar correos.
                            Ideal para servidores que bloquean puertos SMTP (como DigitalOcean o AWS).
                        </p>
                        <ul>
                            <li>✅ No requiere SMTP</li>
                            <li>✅ Funciona sobre HTTPS (puerto 443)</li>
                            <li>✅ Compatible con Community y Enterprise</li>
                        </ul>
                    </div>

                </xpath>
            </field>
        </record>

    </data>
</odoo>
