<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Menú principal -->
        <menuitem id="menu_novasur_services_root"
                  name="Servicios Digitales"
                  sequence="10"
                  groups="sales_team.group_sale_manager"/>

        <!-- Submenú Credenciales -->
        <menuitem id="menu_service_credentials"
                  name="Credenciales"
                  parent="menu_novasur_services_root"
                  action="action_service_credentials"
                  sequence="10"
                  groups="sales_team.group_sale_manager"/>

        <!-- Acción -->
        <record id="action_service_credentials" model="ir.actions.act_window">
            <field name="name">Credenciales de Servicios</field>
            <field name="res_model">service.credentials</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="context">{'search_default_available': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Crear una nueva credencial de servicio digital
                </p>
                <p>
                    Gestiona las credenciales de tus servicios digitales como Spotify, Netflix, YouTube, etc.
                </p>
            </field>
        </record>

        <!-- Vista Kanban -->
        <record id="view_service_credentials_kanban" model="ir.ui.view">
            <field name="name">service.credentials.kanban</field>
            <field name="model">service.credentials</field>
            <field name="arch" type="xml">
                <kanban default_group_by="state" class="o_kanban_small_column">
                    <field name="id"/>
                    <field name="login"/>
                    <field name="product_id"/>
                    <field name="state"/>
                    <field name="partner_id"/>
                    <field name="assign_date"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div class="oe_kanban_global_click o_kanban_record_has_image_fill">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <field name="login"/>
                                        </strong>
                                        <div class="o_kanban_record_subtitle text-muted">
                                            <field name="product_id"/>
                                        </div>
                                    </div>
                                    <span class="badge rounded-pill"
                                          t-attf-class="badge-{{record.state.raw_value == 'available' ? 'success' : record.state.raw_value == 'assigned' ? 'info' : record.state.raw_value == 'expired' ? 'danger' : 'warning'}}">
                                        <field name="state"/>
                                    </span>
                                </div>
                                <div class="o_kanban_record_body">
                                    <field name="partner_id" invisible="not partner_id"/>
                                    <div t-if="record.assign_date.raw_value" class="text-muted small">
                                        Asignado: <field name="assign_date"/>
                                    </div>
                                </div>
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_left">
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                    <div class="oe_kanban_bottom_right">
                                        <button name="action_reset_account" 
                                                type="object" 
                                                class="btn btn-sm btn-primary"
                                                invisible="state != 'pending_reset'"
                                                title="Reiniciar cuenta">
                                            <i class="fa fa-refresh"/> Reiniciar
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <!-- Vista Árbol -->
        <record id="view_service_credentials_tree" model="ir.ui.view">
            <field name="name">service.credentials.tree</field>
            <field name="model">service.credentials</field>
            <field name="arch" type="xml">
                <tree string="Credenciales" 
                      decoration-success="state == 'available'" 
                      decoration-info="state == 'assigned'" 
                      decoration-danger="state == 'expired'"
                      decoration-warning="state == 'pending_reset'">
                    <field name="product_id"/>
                    <field name="login"/>
                    <field name="password" password="True" optional="hide"/>
                    <field name="state" widget="badge" 
                           decoration-success="state == 'available'"
                           decoration-info="state == 'assigned'"
                           decoration-danger="state == 'expired'"
                           decoration-warning="state == 'pending_reset'"/>
                    <field name="partner_id" optional="hide"/>
                    <field name="sale_order_id" optional="hide"/>
                    <field name="assign_date" optional="show"/>
                    <field name="expire_date" optional="hide"/>
                    <field name="company_id" groups="base.group_multi_company" optional="hide"/>
                </tree>
            </field>
        </record>

        <!-- Vista Formulario -->
        <record id="view_service_credentials_form" model="ir.ui.view">
            <field name="name">service.credentials.form</field>
            <field name="model">service.credentials</field>
            <field name="arch" type="xml">
                <form string="Credencial de servicio digital">
                    <header>
                        <button name="action_mark_pending_reset" 
                                string="Marcar para reinicio" 
                                type="object"
                                class="btn-warning"
                                invisible="state != 'assigned'"/>
                        <button name="action_reset_account" 
                                string="Reiniciar cuenta" 
                                type="object"
                                class="btn-primary"
                                invisible="state != 'pending_reset'"/>
                        <button name="action_mark_expired" 
                                string="Marcar como expirado" 
                                type="object"
                                class="btn-danger"
                                invisible="state not in ('assigned', 'available')"/>
                        <button name="action_make_available" 
                                string="Marcar como disponible" 
                                type="object"
                                class="btn-success"
                                invisible="state not in ('expired', 'pending_reset') or sale_line_id"/>
                        <button name="action_resend_credential_email" 
                                string="Reenviar Email" 
                                type="object"
                                class="btn-info"
                                icon="fa-envelope"
                                invisible="state != 'assigned'"
                                groups="base.group_system"/>
                        <field name="state" widget="statusbar" 
                               statusbar_visible="available,assigned,expired"/>
                    </header>
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-archive">
                                <field name="active" widget="boolean_button" options="{'terminology': 'archive'}"/>
                            </button>
                        </div>
                        <group>
                            <group name="service_info" string="Información del Servicio">
                                <field name="product_id" options="{'no_create': True, 'no_open': True}"/>
                                <field name="login" placeholder="usuario@ejemplo.com"/>
                                <field name="password" password="True" placeholder="Ingrese la contraseña"/>
                            </group>
                            <group name="assignment_info" string="Información de Asignación">
                                <field name="assign_date" readonly="1"/>
                                <field name="expire_date"/>
                                <field name="sale_line_id" readonly="1" 
                                       invisible="not sale_line_id"
                                       options="{'no_create': True}"/>
                                <field name="sale_order_id" readonly="1" 
                                       invisible="not sale_order_id"/>
                                <field name="partner_id" readonly="1" 
                                       invisible="not partner_id"/>
                            </group>
                        </group>
                        <group name="other_info" string="Otra Información">
                            <field name="company_id" groups="base.group_multi_company" 
                                   options="{'no_create': True}"/>
                            <field name="notes" placeholder="Notas adicionales sobre esta credencial..."/>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" groups="base.group_user"/>
                        <field name="activity_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>

        <!-- Vista de búsqueda -->
        <record id="view_service_credentials_search" model="ir.ui.view">
            <field name="name">service.credentials.search</field>
            <field name="model">service.credentials</field>
            <field name="arch" type="xml">
                <search string="Buscar Credenciales">
                    <field name="login" string="Login/Email"/>
                    <field name="product_id"/>
                    <field name="partner_id"/>
                    <field name="sale_order_id"/>
                    <separator/>
                    <filter string="Disponibles" name="available" 
                            domain="[('state', '=', 'available')]"/>
                    <filter string="Asignadas" name="assigned" 
                            domain="[('state', '=', 'assigned')]"/>
                    <filter string="Expiradas" name="expired" 
                            domain="[('state', '=', 'expired')]"/>
                    <filter string="Pendientes de reinicio" name="pending_reset" 
                            domain="[('state', '=', 'pending_reset')]"/>
                    <separator/>
                    <filter string="Archivadas" name="inactive" 
                            domain="[('active', '=', False)]"/>
                    <separator/>
                    <group expand="0" string="Agrupar por">
                        <filter string="Servicio" name="group_product" 
                                context="{'group_by': 'product_id'}"/>
                        <filter string="Estado" name="group_state" 
                                context="{'group_by': 'state'}"/>
                        <filter string="Cliente" name="group_partner" 
                                context="{'group_by': 'partner_id'}"/>
                        <filter string="Compañía" name="group_company" 
                                context="{'group_by': 'company_id'}" 
                                groups="base.group_multi_company"/>
                    </group>
                </search>
            </field>
        </record>

    </data>
</odoo>
